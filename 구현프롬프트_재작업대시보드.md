# [Cursor AI 구현 프롬프트] AI기반 재작업 통합 관리 대시보드

> 이 문서를 Cursor AI(Agent 모드)에 그대로 붙여넣어 프로젝트 생성부터 구현까지 진행하세요.
> 아래 내용은 실제 업무 마스터 파일 분석과 담당자 인터뷰를 기반으로 확정된 요구사항입니다. 임의로 항목을 축소/변경하지 말고, 불명확한 부분은 "TODO" 주석으로 남기고 다음 지시를 기다리세요.

---

## 0. 역할 지시

당신은 생산관리팀의 재작업(완제품/대여) 현황을 관리하는 **웹 기반 대시보드**를 만드는 풀스택 개발자입니다. 현재는 담당자 1인이 엑셀로 수작업 관리하는 업무를, 다수가 조회 가능한 시스템으로 전환하는 것이 목표입니다. 아래 명세를 기준으로 **Phase 1부터 순서대로** 구현하세요. 각 Phase 완료 시 실행 가능한 상태로 커밋하고, 다음 Phase로 넘어가기 전에 결과를 요약 보고하세요.

---

## 1. 기술 스택

- **언어/프레임워크**: Python 3.11+, Streamlit (UI/대시보드)
- **DB**: SQLite (파일 기반, `data/rework.db`) — 추후 PostgreSQL 전환을 염두에 두고 SQL은 표준 문법 위주로 작성
- **데이터 처리**: pandas, xlrd(레거시 .xls 읽기), openpyxl(xlsx 읽기/쓰기)
- **인증**: 자체 로그인(회원가입 승인제), 비밀번호는 해시(bcrypt 등) 저장
- **차트**: Streamlit 내장 차트(plotly 또는 altair) 사용

---

## 2. 프로젝트 구조

```
rework-dashboard/
├── app/
│   ├── main.py                  # Streamlit 엔트리포인트
│   ├── db.py                    # DB 연결/세션
│   ├── models.py                # 테이블 정의
│   ├── auth.py                  # 로그인/승인/권한
│   ├── pages/
│   │   ├── 1_개요.py
│   │   ├── 2_업로드_검수.py
│   │   ├── 3_재고현황.py
│   │   ├── 4_완료이력.py
│   │   ├── 5_비용리포트.py
│   │   ├── 6_설정.py
│   │   └── 7_마스터다운로드.py
│   ├── migration/
│   │   ├── schema.sql
│   │   ├── mapping_config.py
│   │   └── migrate_master.py
│   └── services/
│       ├── parsing.py           # 비고란 파싱, 이상치 검증
│       ├── inventory.py         # 재고/장기재고 계산
│       ├── cost_report.py       # 비용 리포트 생성
│       └── settings_service.py
├── data/
│   ├── raw/                     # 원본 마스터 파일 보관
│   └── rework.db
├── tests/
├── requirements.txt
└── README.md
```

---

## 3. 데이터베이스 스키마

`app/migration/schema.sql` 에 아래 DDL을 그대로 작성하세요.

```sql
CREATE TABLE rework_items (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    category        TEXT NOT NULL CHECK (category IN ('완제품', '대여')),
    source_no       INTEGER,
    입고일           DATE,
    담당자           TEXT,
    담당팀           TEXT,
    site            TEXT,
    재작업월         TEXT,
    재작업일         DATE,
    품목분류         TEXT,
    모델명           TEXT,
    serial          TEXT,
    변경모델명       TEXT,
    변경serial       TEXT,
    투입공수         REAL,
    작업주체         TEXT,
    입고수량         INTEGER,
    완료수량         INTEGER,
    구분             TEXT,
    재작업내용       TEXT,
    비고_원문        TEXT,
    status          TEXT NOT NULL DEFAULT '확정' CHECK (status IN ('검수대기', '확정', '완료')),
    created_at      TEXT DEFAULT (datetime('now')),
    updated_at      TEXT DEFAULT (datetime('now')),
    updated_by      TEXT
);
CREATE INDEX idx_rework_category ON rework_items(category);
CREATE INDEX idx_rework_status ON rework_items(category, status);
CREATE INDEX idx_rework_입고일 ON rework_items(입고일);
CREATE INDEX idx_rework_재작업일 ON rework_items(재작업일);

CREATE TABLE settings (
    key             TEXT PRIMARY KEY,
    category        TEXT,
    value           TEXT NOT NULL,
    updated_at      TEXT DEFAULT (datetime('now'))
);

CREATE TABLE managers (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    name            TEXT NOT NULL UNIQUE,
    is_active       INTEGER NOT NULL DEFAULT 1,
    created_at      TEXT DEFAULT (datetime('now'))
);

CREATE TABLE site_flags (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    rework_item_id  INTEGER REFERENCES rework_items(id),
    original_value  TEXT,
    flag_reason     TEXT,
    resolved        INTEGER NOT NULL DEFAULT 0,
    resolved_by     TEXT,
    resolved_at     TEXT
);

CREATE TABLE users (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    company         TEXT,
    name            TEXT NOT NULL,
    team            TEXT,
    position        TEXT,
    login_id        TEXT NOT NULL UNIQUE,
    password_hash   TEXT NOT NULL,
    role            TEXT NOT NULL DEFAULT '일반' CHECK (role IN ('관리자', '일반')),
    approved        INTEGER NOT NULL DEFAULT 0,
    can_upload      INTEGER NOT NULL DEFAULT 0,
    created_at      TEXT DEFAULT (datetime('now'))
);

CREATE TABLE upload_logs (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    category        TEXT NOT NULL,
    uploaded_by     TEXT,
    file_name       TEXT,
    row_count       INTEGER,
    status          TEXT NOT NULL DEFAULT '검수중' CHECK (status IN ('검수중', '확정', '실패')),
    note            TEXT,
    created_at      TEXT DEFAULT (datetime('now'))
);
```

**기본 설정값 시드**
```
long_term_threshold_days / 완제품 / "180"
long_term_threshold_days / 대여   / "180"
excluded_status_values   / NULL  / '["폐기"]'
minute_wage_rate         / NULL  / "547"
model_group_options      / NULL  / '["DISPLAY"]'
work_type_options        / NULL  / '["재작업"]'
```

---

## 4. 기존 마스터 파일 매핑 (마이그레이션용)

두 원본 마스터는 컬럼명이 다르므로 카테고리별 매핑을 구분한다. 컬럼명에 개행문자(`\n`)가 포함되어 있으니 `pandas.read_excel` 결과를 실제로 확인 후 매핑할 것.

```python
COLUMN_MAPPING = {
    "완제품": {
        "sheet_name": "완제품재작업현황", "header_row": 2,
        "columns": {
            "No.": "source_no", "입고일": "입고일", "담당자": "담당자", "담당팀": "담당팀",
            "사이트명": "site", "재작업월": "재작업월", "재작업일": "재작업일",
            "품목\n분류": "품목분류", "모델명": "모델명", "Ser.": "serial",
            "변경모델명": "변경모델명", "변경Ser.": "변경serial",
            "투입\n공수": "투입공수", "작업\n주체": "작업주체",
            "입고\n수량": "입고수량", "완료\n수량": "완료수량",
            "구분": "구분", "재작업내용": "재작업내용", "비고": "비고_원문",
        },
    },
    "대여": {
        "sheet_name": "대여작업현황", "header_row": 2,
        "columns": {
            "No.": "source_no", "입고일": "입고일", "담당자": "담당자", "담당팀": "담당팀",
            "대여처": "site", "재작업월": "재작업월", "재작업일": "재작업일",
            "품목\n분류": "품목분류", "모델명": "모델명", "Ser.": "serial",
            "변경모델명": "변경모델명", "변경Ser.": "변경serial",
            "투입\n공수": "투입공수", "작업\n주체": "작업주체",
            "입고\n수량": "입고수량", "완료\n수량": "완료수량",
            "구분": "구분", "재작업내용": "재작업내용", "비고": "비고_원문",
        },
    },
}
```

**마이그레이션 처리 규칙**
- 완전히 빈 행이 연속 3행 이상 나타나는 지점부터는 데이터 없음으로 간주하고 컷
- 날짜: `pd.to_datetime(errors="coerce")`, 숫자: `pd.to_numeric(errors="coerce")`
- `재작업일 IS NULL` → `status='확정'`(현재 재고) / `재작업일 IS NOT NULL` → `status='완료'`
- 재실행 시 중복 적재 방지 (`upload_logs`에 `note='initial_migration'` 존재 여부로 가드)
- 마이그레이션 후 카테고리별 적재 행수, 날짜 파싱 실패 건수를 콘솔에 리포트

---

## 5. 핵심 비즈니스 로직

### 5.1 업로드 시 비고란 파싱
- ERP 원본의 "비고"란은 `SITE, 담당자, 시리얼번호` 순서로 쉼표 구분되어 있음 → split 후 3개 필드로 분리
- 담당자명: `managers` 테이블(활성 항목)과 대조, 없으면 하이라이트(신규 등록 가능 UI 제공)
- SITE명: 값이 비어있거나 지나치게 짧거나(2자 미만) 숫자/특수문자만인 경우 이상치로 하이라이트
- 3개 필드 중 하나라도 누락되면 하이라이트
- **업로드 직후 자동 반영 금지** — 담당자의 1회 검수 확인 후에만 `status='확정'`으로 전환

### 5.2 재고/장기재고 판정
```python
is_inventory = (재작업일 is None) and (구분 not in excluded_status_values)
is_long_term = is_inventory and (오늘 - 입고일).days >= threshold_days[category]
```

### 5.3 월별 지표
- 월별 입고수량 = `입고일`의 연월 기준 합계
- 월재작업수량(=월별 완료수량) = `재작업일`의 연월 기준 합계 (표시 지표명은 "월재작업수량")

### 5.4 완료 처리
- 담당자가 완료 체크 또는 완료일 수기 입력 시 `재작업일`, `완료수량`, `구분` 갱신 → `status='완료'`
- 삭제하지 않고 완료 이력 테이블 조회에서 항상 확인 가능해야 함 (재고 집계에서만 제외)

### 5.5 비용 리포트
```
금액 = 투입공수 × 분당임율(기본 547) × 60
```
- 완제품 → **"재작업현황"** 시트 포맷: `계획날짜(=재작업일의 연월), 작업날짜(=재작업일), MODEL군(기본 DISPLAY, 설정에서 추가가능), 작업구분(기본 재작업, 설정에서 추가가능), 귀책부서(=담당팀), 금액` / 나머지(사유, 귀책사유정리, 전산공수반영여부, LOSS실공수, 품질보증팀귀책확정)는 공란
- 대여 → **"대여반납"** 시트 포맷: 기존 마스터 컬럼 그대로 + "금액" 컬럼 삽입
- 기간(from~to, 재작업일 기준) 설정 후 **하나의 xlsx 파일에 시트 2개**로 다운로드

---

## 6. 화면 명세

### 6.1 공통 레이아웃
- 상단 헤더: 화이트 배경, 좌측에 ATEC 로고(제공 이미지 파일 그대로 삽입, 재제작 금지) + 타이틀
- 최상단 탭: [완제품재작업] [대여재작업] 카테고리 전환
- 좌측 또는 상단 메뉴: 개요 / 업로드·검수 / 재고현황 / 완료이력 / 비용리포트 / 설정 / 마스터다운로드
- 색상 팔레트(다크톤 금지, 파스텔 배경 + 진한 텍스트):

| 용도 | 배경 | 텍스트 |
|---|---|---|
| 페이지 배경 | `#F7F9FB` | — |
| 카드/헤더 | `#FFFFFF` | — |
| 정보(총 재고 등) | `#EAF3FB` | `#0C447C` |
| 주의(장기재고) | `#FAEEDA` | `#633806` |
| 완료/정상 | `#E1F5EE` | `#085041` |
| 경고(검수대기) | `#FCEBEB` | `#791F1F` |

### 6.2 페이지별 요구사항

**① 개요**
- KPI 카드: 총 재고수량 / 장기재고 건수 / 이번달 입고수량 / 이번달 완료수량 / 검수대기 건수
- 장기재고 연령 구간별(0~3/3~6/6~12/12+개월) 막대차트
- 담당팀별·SITE별 재고 비중 도넛/막대차트
- 최근 12개월 입고 vs 완료 추이 라인차트

**② 업로드·검수**
- 파일 업로드 위젯(xlsx/xls)
- 파싱 결과 미리보기 테이블, 이상치 행 하이라이트(배경색 `#FCEBEB`)
- 행별 인라인 수정 가능, 담당자명 신규 등록 버튼
- "확정 반영" 버튼 클릭 시 마스터 반영 + 입고일 자동 기록

**③ 재고현황**
- 필터: 모델명/담당자/SITE/담당팀/구분/입고기간, 장기재고만 보기 토글
- 입고일 개별수정 / 체크박스 다건 선택 후 일괄수정
- 완료 처리(체크박스 + 완료일 입력)
- "필터링된 결과 다운로드" 버튼 (현재 화면에 보이는 행만 xlsx로)

**④ 완료이력**
- 기간 필터, 완료 건 목록 테이블
- 월별 완료수량/입고수량 비교 차트

**⑤ 비용리포트**
- 기간(from~to) 선택
- 분당임율 표시(설정값 링크)
- "리포트 생성 및 다운로드" 버튼 → 위 5.5 규칙에 따라 2시트 xlsx 생성

**⑥ 설정 (관리자 전용)**
- 장기재고 기준일수(완제품/대여 각각 입력)
- 재고 제외 "구분" 값 다중선택
- 담당자명 목록 편집(추가/비활성화)
- MODEL군 / 작업구분 옵션 추가
- 분당임율 입력
- 사용자 승인 대기 목록 + 승인/거부, 역할 변경, 개별 확장 권한(업로드 등) 토글

**⑦ 마스터 다운로드**
- 완제품/대여 마스터 전체를 원본과 동일한 컬럼 구조로 xlsx 다운로드 (백업용)

### 6.3 로그인/가입 화면
- 로그인: 아이디/비밀번호, "승인된 계정만 이용 가능" 안내 문구
- 회원가입: 회사명/이름/팀/직책/아이디/비밀번호 입력 → 제출 후 "관리자 승인 대기" 상태로 저장

---

## 7. 구현 순서 (Phase)

1. **Phase 1**: DB 스키마 생성 + 기존 마스터 파일 2종 마이그레이션 스크립트 (섹션 3, 4)
2. **Phase 2**: 로그인/회원가입/승인 흐름 (섹션 6.3, AUTH 전체)
3. **Phase 3**: 업로드 → 비고란 파싱 → 검수 화면 (섹션 5.1, 6.2-②)
4. **Phase 4**: 재고현황 화면 (필터/수정/완료처리/다운로드) (섹션 5.2, 5.4, 6.2-③)
5. **Phase 5**: 개요 대시보드(KPI/차트) + 완료이력 화면 (섹션 5.3, 6.2-①④)
6. **Phase 6**: 비용 리포트 생성 (섹션 5.5, 6.2-⑤)
7. **Phase 7**: 설정 화면 + 마스터 다운로드 (섹션 6.2-⑥⑦)
8. **Phase 8**: 통합 테스트, 실제 마스터 데이터 기준 검증

각 Phase 완료 후 다음을 확인하고 보고하세요:
- 해당 Phase의 요구사항 ID(예: UPLOAD-01~10)가 모두 충족되었는지
- 실행 가능한 상태인지 (`streamlit run app/main.py`로 구동 확인)

---

## 8. 인수 기준 (완료 판정 기준)

- 기존 마스터 2종(완제품 5,918행/대여 1,664행)이 마이그레이션 후 누락·중복 없이 조회됨
- 재작업일 유무에 따른 재고/완료 상태 분류 100% 일치
- 비고란 파싱 테스트 20건 이상에서 SITE/담당자/시리얼 정상 분리 및 이상치 하이라이트 확인
- 장기재고 기준일수 변경 시 대시보드 수치 즉시 반영
- 비용 계산 검증: 투입공수 0.8, 분당임율 547 → 26,256원 산출 확인 (기존 수기 산출값과 일치)
- 조회자(일반) 계정으로 로그인 시 수정/설정 메뉴 비노출·접근 차단 확인
- 필터링 후 다운로드한 파일 행 수 = 화면 표시 행 수

---

## 9. 주의사항

- ERP와 직접 연동하지 않음 (파일 업로드 방식 유지)
- 업로드 자동 파싱 결과는 반드시 사람의 1회 검수를 거쳐야 마스터에 반영됨 (자동 확정 금지)
- "구분" 값에 따른 재고 제외 처리는 하드코딩하지 말고 설정 테이블 기반으로 구현
- 완료된 항목은 절대 삭제하지 말 것 (이력 보존)
- 불확실한 사항은 임의로 추정하여 구현하지 말고 TODO로 남길 것
