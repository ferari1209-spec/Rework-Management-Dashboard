# 구현프롬프트 재작업대시보드 챗gpt_260916

기준일: 2026-09-16 | 개정판: 2.0 | 대상: 개발·유지보수 담당자 및 구현 AI

## 문서 사용 방법

이 문서는 첨부된 ‘구현프롬프트_재작업대시보드.md’를 지금까지의 확정 요청과 현재 코드에 맞춰 갱신한 구현 지침이다. 신규 프로젝트를 무조건 생성하는 초기 프롬프트가 아니라, 기존 시스템을 보존하며 기능을 유지·확장하기 위한 기준으로 사용한다. PDF와 MD는 같은 본문을 제공한다.

기존 프로젝트가 있으면 먼저 코드를 확인하고 차이가 있는 부분만 수정하라. 운영 DB, 승인 계정, 원본 마스터, 저장된 설정을 초기화하거나 덮어쓰지 말라. 원본 문서에 있는 이전 요구보다 이 문서의 최신 규칙을 우선 적용하라. 별도 변경 승인이 없는 기능을 임의로 축소하지 말라. 실제 코드와 명세가 다르면 차이와 영향을 보고하고, 화면 표시만으로 완료를 주장하지 말라.

### 원본 대비 핵심 정정

- 제목: ‘AI기반’을 제외한 ‘재작업 통합 관리 대시보드’로 통일.
- 완료 판정: 재작업일 또는 완료수량 양수. 공수 누락은 완료 취소 사유가 아님.
- 이관: 연속 빈 행 3개 이후 절단 규칙 폐기. 중간 공백 뒤 유효 데이터 보존.
- 원본의 완제품 5,918행·대여 1,664행을 고정 인수 수치로 사용하지 않음. 실제 파일의 유효 업무행과 대조.
- 역할: 관리자·담당자·일반 3종과 사용자별 메뉴 권한. 이메일 가입·세션 유지·관리자 코드 재설정 추가.
- KPI: 검수대기 카드 제거, 전월 비교·증감·상세 팝업 추가.
- 표: 부분 검색, 개별/전체 선택, 선택 출력, 권한 있는 재고 삭제와 삭제 이력 추가.
- 비용 출력: 재작업현황 16열로 개정. ‘무/재작업 현황’은 원본 ‘구분’ 연결.
- 화면: 옅은 회색 바탕·입체 카드·간결한 필터·차트 수치 분리 배치로 갱신.
- AI: 외부 전송 없는 통계 분석에 개인 키 기반 선택적 AI 해설 추가.

### 기술 스택과 코드 기준

Python 3.11 이상을 기준으로 하며 현재 확인 환경은 Python 3.12이다. Streamlit, pandas, SQLite, xlrd, openpyxl, bcrypt, Plotly를 사용한다. 정확한 설치 범위는 requirements.txt를 따른다. 현재 DB는 data/rework.db이고 원본은 data/raw에 보관한다. 외부 AI 호출은 Python 표준 urllib를 사용한다.

- 진입점 및 DB: app/main.py, app/db.py, app/data_access.py.
- 계정 및 공통 화면: app/auth.py, app/ui.py, app/dashboard.css, app/table_ui.py, app/chart_labels.py.
- 화면: app/pages의 개요·업로드_검수·재고현황·완료이력·비용리포트·설정·마스터다운로드·AI분석.
- 이관: app/migration/schema.sql, mapping_config.py, migrate_master.py.
- 업무 규칙: app/services/completion.py, inventory.py, cost_report.py, settings_service.py, permissions.py.
- AI: app/services/analysis_service.py, app/services/ai_explanation.py, app/ai_ui.py.
- 검증: tests 및 scripts/check_cost_report_revision.py. 초기 설계에 적힌 models.py 생성은 필수 조건이 아니다.

## 1. 목적과 범위

완제품 및 대여반납 제품의 입고, 검수, 재고, 재작업 완료, 공수 및 비용을 통합 관리한다. 기존 마스터 파일의 업무 데이터를 보존하고, 조회·수정·출력과 사용자별 권한을 제공한다.

- 시스템 표시 명칭은 **재작업 통합 관리 대시보드**로 한다. 화면 제목에 ‘AI기반’을 사용하지 않는다.
- 관리 카테고리는 **완제품재작업**, **대여재작업**으로 구분한다.
- 메뉴는 개요, 업로드·검수, 재고현황, 완료이력, 비용리포트, 마스터 다운로드, 설정, AI 분석으로 구성한다.
- 통계·규칙 기반 이상탐지와 개인 OpenAI API 키를 이용한 선택적 AI 해설을 제공한다. 미래 예측 모델, ERP 직접 API 연동, 메일 자동 발송은 제공하지 않는다.
- 최초 이관 당시의 행 수와 오늘의 운영 데이터 건수는 구별한다. 본 문서는 특정 운영 건수를 고정 요구사항으로 삼지 않는다.

## 2. 실행 및 운영 환경

### SYS-01 실행

Python·Streamlit 기반 웹 화면과 SQLite 저장소를 사용한다. 현재 실행 주소는 `http://127.0.0.1:8511/rework-dashboard/`이며 다른 프로젝트와 충돌하지 않도록 전용 포트와 경로를 사용한다.

- `대시보드 실행.cmd`로 서버를 실행한다.
- PC 재시작 또는 서버 종료 후에는 다시 실행해야 한다. 자동 시작은 현재 제공하지 않는다.
- 127.0.0.1은 서버가 실행 중인 PC에서만 접속 가능한 주소이다. 승인된 사용자의 공용 웹 접속은 외부 호스팅·도메인·HTTPS 구성이 필요한 별도 배포 대상이다. 회사 내부 서버 사용은 필수 조건이 아니다.

### SYS-02 저장과 백업

- 업무 데이터·사용자·설정은 `data/rework.db`에 저장한다.
- 원본 업로드 파일은 `data/raw`에 보관한다.
- 원본 파일 자체를 덮어쓰지 않는다.
- 운영 DB를 초기화하거나 재이관하여 기존 운영 데이터를 대체하지 않는다.
- 비용리포트 2026-09-15 변경 전 코드는 `data/backups/cost_report_20260915`에 보관되어 있다. 이 백업은 비용리포트 코드의 원복용이며 전체 시스템 백업과는 다르다.

## 3. 로그인·회원가입·비밀번호

### AUTH-01 최초 관리자

최초 실행 시 관리자 계정을 한 번 설정한다. 이후에도 관리자는 기존 사용자에게 관리자 역할을 부여할 수 있다. 승인된 관리자가 최소 한 명 존재해야 한다.

### AUTH-02 회원가입

회사명, 이름, 팀, 직책, 이메일 아이디, 비밀번호 및 비밀번호 확인을 입력한다.

- 아이디는 유효한 이메일 형식이어야 하며 중복을 허용하지 않는다.
- 비밀번호는 8자 이상, UTF-8 기준 72바이트 이하로 제한한다.
- 비밀번호 확인이 일치해야 가입 신청이 가능하다.
- 신청 계정은 승인 대기 상태로 등록되며 관리자 승인 후 사용할 수 있다.
- 예시 문구: 회사명 ‘에이텍컴퓨터’, 이름 ‘홍길동’, 팀 ‘생산관리팀’, 직책 ‘책임’, 이메일 ‘name@company.com’, 비밀번호 ‘8자 이상 입력’, 확인 ‘비밀번호 재입력’.
- 사용자 수동 등록 기능은 최종 요청에 따라 제거하고 **선택 사용자 수정**으로 대체한다.
- 기존 아이디를 이메일로 전환하는 로그인 화면 항목은 제공하지 않는다.

### AUTH-03 로그인과 유지

- 이메일·비밀번호로 로그인하며 비밀번호 입력 후 Enter로 로그인할 수 있다.
- 새로고침 시 유효한 로그인 세션을 복원한다. 세션 유효기간은 8시간이다.
- ‘아이디 저장’은 브라우저에 이메일만 30일간 보관한다. 비밀번호를 저장하지 않는다.
- 로그아웃은 로그인 토큰을 무효화한다.
- 비밀번호는 bcrypt 해시로 저장하고 세션 토큰은 서버에 해시 형태로 관리한다.

### AUTH-04 비밀번호 찾기

로그인 옆에 같은 청록색의 짧은 ‘비밀번호 찾기’ 버튼을 두고 재설정 팝업을 연다.

- 관리자 본인 확인 후 코드 발급까지 시간이 소요될 수 있음을 안내한다.
- 관리자는 요청자 본인 확인 체크와 자신의 비밀번호 검증 후 코드를 발급한다.
- 관리자가 코드를 직접 전달한다. 시스템은 이메일을 자동 발송하지 않는다.
- 코드는 30분간 1회 사용 가능하다. 재발급하면 이전 코드는 무효화된다.
- 사용자는 등록 이메일·코드·새 비밀번호·확인을 입력한다.
- 비밀번호 변경 시 기존 로그인 세션을 모두 해제한다.

## 4. 역할 및 개별 권한

### PERM-01 기본 역할

- **관리자:** 모든 메뉴 및 작업 권한. 설정은 관리자만 접근한다.
- **담당자:** 조회 및 다운로드. 기본적으로 등록·수정·삭제는 불가하다.
- **일반:** 조회만 가능하며 시스템이 제공하는 다운로드 작업은 기본적으로 불가하다.

### PERM-02 메뉴별 권한

관리자는 개별 사용자를 선택하여 업로드·검수, 재고현황, 완료이력, 비용리포트, 마스터 다운로드, AI 분석의 조회·다운로드 권한을 설정한다.

- 업로드·검수와 재고현황은 등록·수정·삭제 작업 권한도 설정한다.
- 현재 등록·수정·삭제는 하나의 묶음 권한이다. 삭제만 따로 분리하는 권한은 제공하지 않는다.
- 조회 권한을 해제하면 해당 메뉴의 작업도 제한한다.
- 관리자 역할에는 모든 권한이 적용된다.
- ‘역할 기본값 적용’으로 기존 개별 권한을 초기화할 수 있다.
- 기존 계정의 업로드 허용값은 호환 유지하며 관리자가 권한을 다시 저장하여 조정할 수 있다.
- 메뉴 노출과 페이지 진입을 검사하며, 업로드·재고 변경은 서버 처리 함수에서도 권한을 검사한다.
- 설정 권한은 개별 메뉴 권한으로 부여할 수 없다.
- 조회 권한은 화면 데이터 열람을 허용한다는 의미이다. 다운로드 버튼 제어를 화면 캡처나 열람 데이터 복제 방지 기능으로 해석하지 않는다.

## 5. 데이터 입력과 검수

### DATA-01 원본 및 업로드

- 완제품·대여 마스터 및 지원하는 ERP 입고 장표의 XLS/XLSX를 읽는다.
- 마스터는 기존 입고일·완료일·공수 등의 값을 보존한다.
- ERP 입고 장표는 업로드일을 입고일로 사용한다.
- 번호만 있는 빈 행 및 완전 공백 행은 업무 데이터에서 제외한다.
- 인식하지 못한 형식은 임의로 반영하지 않는다.
- 파일 내용 해시와 카테고리로 중복 반영을 차단한다. 내용이 바뀐 파일은 다른 파일로 인식하므로 같은 행의 재입력을 자동 병합하는 기능은 아니다.

### DATA-02 검수

SITE·담당자·시리얼 등 파싱 결과와 누락·숫자·날짜 이상 항목을 확인하고 수정할 수 있어야 한다. 검수 확인 후 반영하며 반영 행과 업로드 이력은 원자적으로 저장한다. 업로드 권한이 없는 사용자는 허용된 조회만 제공한다.

### DATA-03 담당자별 담당팀

- 설정의 담당자 목록에 담당자명·담당팀·사용 여부를 관리한다.
- 활성 담당자의 이름이 일치하고 원본 담당팀이 비어 있으면 등록된 담당팀을 자동 입력한다.
- 마스터에 이미 담당팀이 입력되어 있으면 업로드 시 유지한다.
- 담당자 목록 저장 시 기존 업무 데이터의 빈 담당팀에도 동일 규칙을 적용한다.
- 재고 편집에서 담당자 변경 시 해당 담당자의 팀을 연계한다.
- 이름은 부분 검색이 아닌 이름 일치로 연결한다. 동명이인 구분 기능은 현재 없으며 동일 담당자명 중복 등록을 허용하지 않는다.

## 6. 완료·재고·장기재고 판정

### RULE-01 완료

유효한 재작업일이 있거나 완료수량이 0보다 크면 완료로 판정한다. **투입공수가 없어도 완료수량이 1이면 완료이다.** 완료 표시가 있는 항목을 공수 누락 때문에 미완료로 되돌리지 않는다.

### RULE-02 재고

미완료 항목 중 설정의 제외 구분 및 검수대기 항목을 제외해 재고를 구성한다. 재고수량은 입고수량 합계이며 품목 건수와 구분한다.

### RULE-03 장기재고

- 입고일로부터의 경과일이 카테고리별 기준 이상이면 장기재고이다.
- 초기 기본 기준은 180일이며 관리자가 변경할 수 있다. 화면·집계에는 저장된 설정값을 사용한다.
- 입고일 미등록 항목은 재고에는 포함하고 장기재고 판정에서는 제외한다.
- 제외 구분의 초기값은 없음이다. ‘폐기’도 관리자가 제외하도록 설정하지 않았다면 자동 제외하지 않는다.

## 7. 개요

### DASH-01 요약 카드

현재 재고수량, 장기재고, 이번 달 입고, 이번 달 완료의 4개 카드를 같은 너비로 표시한다. 검수대기 카드는 표시하지 않는다.

- 현재 재고와 장기재고는 전월 말 재구성값과 비교한다.
- 입고·완료는 이번 달과 전월 전체 월 합계를 비교한다. 동일 경과일 비교가 아니다.
- 전월 값·증감 수량·증감률을 표시하며 전월 0은 비율 산정 불가로 안내한다.
- 상세보기 팝업에서 현재·전월 내역과 집계 기준을 제공하고 권한에 따라 엑셀 다운로드를 허용한다.

### DASH-02 전월 재고의 한계

전월 말 재고는 입고일·완료일로 재구성한다. 현재의 제외 구분·장기재고 설정을 적용하며 과거 상태 변경 이력 전체를 복원하는 스냅샷은 아니다. 완료수량은 있으나 완료일이 없거나 입고일이 없는 항목은 전월 재고를 확정할 수 없으므로 제외 건수와 기준을 안내한다.

### DASH-03 그래프

- 재고 연령 분포, 현재 재고 상태, 담당팀별 재고, 월별 입고·완료, SITE별 재고 상위 10곳을 제공한다.
- 연령 구간은 0~3개월, 3~6개월, 6~12개월, 12개월 이상, 입고일 미등록으로 구분한다.
- 막대에는 건수·수량, 도넛에는 건수·비율과 중앙 전체 건수를 표시한다.
- 월별 그래프는 선·막대 영역과 수치 영역을 분리한다. 상단 맨 왼쪽에 ‘입고/완료’를 한 번 표시하고 각 월에는 숫자 두 줄을 표시한다.
- 월별 수치표는 별도로 배치하지 않는다. 좁은 화면이나 긴 기간은 가로 스크롤로 수치 간격을 유지한다.
- 월별 입고·완료와 SITE 그래프의 기본 높이를 맞춘다.
- 장기재고 상세와 재작업 유형별 재고 내역을 제공한다.

## 8. 재고현황

### INV-01 조회

모델명·담당자·SITE·담당팀·구분을 같은 줄에 배치한다. 다음 줄에 입고 시작·종료일과 장기재고만 보기 옵션을 배치한다. 부분값 검색은 대소문자 구분 없이 입력 문자열을 포함하는 항목을 조회한다.

### INV-02 선택과 출력

- 각 행에 선택 체크박스를 제공한다.
- 전체 선택·전체 해제는 현재 검색·필터 결과에만 적용한다.
- 조회 결과가 바뀌면 이전 위치의 선택 상태를 재사용하지 않는다.
- 선택 다운로드 및 필터링된 전체 결과 다운로드를 구분한다.
- 전체 결과 다운로드는 전체 선택·전체 해제·부분값 검색과 같은 줄에 배치한다.

### INV-03 수정과 완료 처리

권한 있는 사용자는 허용 필드를 편집하고 저장한다. 관리번호·원문 비고·계산 필드 등은 직접 편집하지 않는다. 선택 행의 입고일 일괄수정과 완료일·구분을 지정한 완료 처리를 제공한다. 완료 처리 후 이력은 보존하고 재고에서 제외한다.

### INV-04 잘못 입력한 항목 삭제

선택 재고를 삭제하기 전에 대상 내역·건수를 보여주고 사유와 확인을 받는다. 삭제 시 권한 및 현재 재고 여부를 재확인한다. 삭제 전 행 데이터와 삭제자·사유·시각을 같은 트랜잭션의 삭제 이력에 보관한다. 삭제 이력은 보관 기능이며 현재 화면에 복원 버튼은 없다.

### INV-05 작업 영역

표 아래에 선택 항목 관리, 정보 수정, 완료 처리의 3개 카드를 나란히 배치한다. 선택 건수·수량을 표시하고 선택 대상이 없으면 선택 작업 버튼을 비활성화한다.

## 9. 완료이력

### HIST-01 목록

완료 상태 항목에 기간 및 부분 검색을 적용한다. 완료일 미등록 포함 옵션은 기본 선택하며, 선택하면 미등록 항목은 기간에 관계없이 보여준다. 완료일 없는 항목은 월별 완료 집계에서 제외한다.

행별 선택·전체 선택·전체 해제·선택 엑셀 다운로드를 제공한다. ‘완료 건수’와 ‘표시 행 수’는 같은 줄로 표시한다.

### HIST-02 월별 그래프

- 기본 기간은 이번 달을 포함한 최근 12개월이다.
- 그래프 시작월·종료월을 별도로 설정할 수 있다.
- 위 목록의 기간·검색과 그래프 기간은 독립적이다.
- 선택한 월 전체를 집계하며 데이터가 없는 월은 0으로 표시한다.
- 시작월이 종료월보다 늦으면 안내하고 그래프를 실행하지 않는다.

## 10. 비용리포트

### COST-01 조회 및 계산

재작업일 시작·종료 기간과 부분 검색으로 완료 항목을 조회한다. 완료일 미등록 항목은 기간별 리포트에서 제외하고 건수를 안내한다.

- 금액 = 투입공수(시간) × 분당임율(원/분) × 60.
- 금액은 행별 원 단위 반올림값을 사용한다.
- 분당임율 기본값은 547원이며 설정에서 변경한다.
- 예: 0.8시간 × 547원 × 60 = 26,256원.
- 공수 미등록은 비용 합계에서 제외한다. 0시간과 미등록을 구분한다.
- 완료 건수·투입공수·재작업 비용을 아이콘이 있는 3개 카드로 표시한다.
- 개별 선택 다운로드는 선택된 표 내역을 출력한다.
- 정식 리포트는 선택 날짜 기간 전체를 출력한다. 화면 부분 검색·행 선택과 정식 리포트 범위를 혼동하지 않도록 안내한다.

### COST-02 재작업현황 시트의 최종 열 순서

계획날짜 → 작업날짜 → MODEL군 → MODEL → 작업구분 → 구분 → SITE → 무/재작업 현황 → 재작업수량 → 재작업공수 → 금액 → 귀책부서 → 귀책사유정리 → 전산공수반영여부 → LOSS실공수 → 품질보증팀귀책확정.

### COST-03 재작업현황 데이터 연결

- 대상은 기간 내 완료된 **완제품** 데이터이다.
- 계획날짜: 재작업일의 연·월 표시.
- 작업날짜: 재작업일.
- MODEL군: 설정의 MODEL군 첫 번째 값.
- MODEL: 원본 모델명의 앞 4자리. 모델명 미등록은 빈 값.
- 작업구분: 설정의 작업구분 첫 번째 값.
- 구분: **생산계획**으로 고정.
- SITE: 원본 사이트명(`site`).
- **무/재작업 현황: 원본 ‘구분’.** 최초 ‘재작업내용’ 연결 요청은 후속 요청으로 변경되었다.
- 재작업수량: 원본 완료수량.
- 재작업공수: 원본 투입공수.
- 금액: 공수 × 분당임율 × 60.
- 귀책부서: 원본 담당팀.
- 귀책사유정리·전산공수반영여부·LOSS실공수·품질보증팀귀책확정: 현재 자동 연결 없이 빈 값 유지.
- 첨부 검토본에서 빨간색으로 표시된 **사유 열은 삭제**한다.

### COST-04 대여반납 시트

기간 내 완료된 대여 데이터에 대해 기존 열 구조를 유지한다. 입고년도, 입고월, 입고일, 담당자, 담당팀, 대여처, 재작업월, 재작업일, 품목분류, 모델명, Ser., 변경모델명, 변경Ser., 투입공수, 금액, 작업주체, 입고수량, 완료수량, 구분, 재작업내용, 비고 순서이다.

## 11. 마스터 다운로드

카테고리별 데이터를 원본 마스터의 열 이름·열 순서에 맞춰 XLSX로 내려받는다. 검수대기 항목은 제외한다. 원본의 매크로·모든 장식·인쇄 설정까지 동일 복제하는 기능은 아니다. 다운로드 권한 없는 계정에는 다운로드를 제공하지 않는다.

## 12. 설정

### SET-01 기본 설정 카드

- 재고 관리 기준: 완제품·대여 기준일수, 재고 제외 구분.
- 비용 계산 기준: 분당임율, 계산식 안내.
- 분류 항목 설정: 비용 엑셀의 MODEL군·작업구분. 콤마 입력은 가능하나 현재 첫 번째 값만 공통 적용한다.
- 기본 설정 저장 버튼은 카드 아래 오른쪽에 둔다.

### SET-02 담당자 관리

넓은 담당자 목록과 옆의 추가 입력 카드를 제공한다. 이름·담당팀·사용 여부를 수정하고, 부분 검색·개별/전체 선택·삭제를 지원한다. 목록 저장과 선택 삭제 버튼을 같은 줄에 둔다. 담당자 목록 삭제는 기존 재작업 이력을 삭제하지 않는다.

### SET-03 사용자 승인 및 수정

사용자 목록에 부분 검색·개별/전체 선택을 제공한다. 선택 승인·수정·삭제 버튼을 한 줄로 배치한다. 수정은 한 명 선택 시 가능하며 회사명·이름·담당팀·직책·이메일을 변경한다. 이메일 형식·중복을 검증한다. 현재 로그인한 계정은 삭제하지 않으며 승인된 관리자를 최소 한 명 유지한다. 사용자 삭제는 기존 업무 이력과 별개이다.

### SET-04 권한과 비밀번호

사용자별 역할·메뉴 권한표를 하나의 카드로 묶고, 비밀번호 재설정은 별도 카드로 제공한다. 권한과 재설정 규칙은 3~4장의 요구사항을 따른다.

## 13. 공통 화면 디자인

### UI-01 브랜드와 배치

- 제공된 에이텍컴퓨터 로고를 로그인·메인 화면에 사용한다.
- 최신 공통 바탕은 옅은 회색 `#F3F5FA`이다. 초기 사진 배경은 현재 표시하지 않는다.
- 메인 제목은 남색 계열 입체 테두리 안에 로고·시스템명·사용자·기준일을 표시한다.
- ‘Technology / ATEC의 기술은 / 새로운 정보통신 문화를 창조합니다.’ 문구는 제목 영역 중앙에 배치한다.
- 표와 그래프는 화면 너비를 활용하고, 필터는 짧고 간결하게 배치한다.

### UI-02 카드와 그래프

옅은 청회색 배경, 강조된 제목 영역, 청록색 포인트, 얇은 테두리와 부드러운 그림자로 통일한다. 비용 카드 숫자는 굵고 단위는 작게 표시한다. 월별 숫자는 선·막대와 떨어진 상단 영역에 배치한다.

### UI-03 표·필터·버튼

- 표 머리글은 옅은 회색으로 표시하고 글씨를 굵게 한다. 현재 표 엔진의 공통 설정 때문에 본문 글씨도 함께 굵게 표시된다.
- 주요 메뉴·검색·날짜·설정·선택·수정·삭제·다운로드에는 기능별 선 아이콘을 사용하고 텍스트를 유지한다.
- 전체 선택·해제·부분 검색·건수를 한 줄에 배치한다. 좁은 화면에서는 줄바꿈이 가능하다.
- 활성 다운로드 버튼은 청록색 배경·흰색 글씨, 비활성은 옅은 회색, 마우스를 올리면 진한 청록색으로 표시한다.
- 로그인·비밀번호 찾기·가입 신청은 동일한 청록색 계열을 사용한다.

## 14. 인수 확인 기준

아래 항목은 납품·변경 후 확인할 기준이다. 본 문서 작성 자체를 모든 환경에서의 인수시험 완료로 간주하지 않는다.

1. 이메일 형식 오류·중복 가입·비밀번호 불일치를 차단하고 승인 전 로그인을 제한한다.
2. Enter 로그인, 새로고침 복원, 아이디 저장, 로그아웃이 동작한다.
3. 비밀번호 재설정 코드 만료·재발급·1회 사용과 기존 세션 해제가 동작한다.
4. 관리자·담당자·일반의 기본 권한, 개별 권한 변경 및 설정 접근 제한이 동작한다.
5. 완료수량 1·공수 없음 항목이 재고에서 제외되고 완료이력에 나타난다.
6. 완료일 없는 완료 항목이 월별 완료·비용에서 제외되고 안내된다.
7. 담당자명만 있는 원본의 빈 담당팀이 자동 입력되며 기존 팀 값은 유지된다.
8. 부분 검색·전체 선택·해제·선택 다운로드가 필터 결과에 맞게 동작한다.
9. 재고 삭제 확인과 삭제 이력이 동작하고 선택하지 않은 행은 유지된다.
10. 그래프 기간 기본 12개월, 사용자 기간 설정, 누락 월 0 표시가 동작한다.
11. 비용리포트 16개 열 순서와 MODEL·생산계획·사이트·원본 구분·완료수량·투입공수 연결이 일치한다.
12. 대여반납 시트가 비용리포트의 완제품 양식 변경으로 영향을 받지 않는다.
13. 좁은 화면·다수 월·0값·큰 수량에서도 레이블이 읽히는지 실제 브라우저에서 확인한다.

## 15. 현행 제한과 향후 협의 대상

- 서버 자동 시작, 외부 호스팅 공용 배포·HTTPS·운영 백업 스케줄은 별도 작업이다.
- 전월 재고는 날짜 기반 재구성으로 실제 과거 스냅샷과 차이가 날 수 있다.
- 담당자 동명이인 식별, 삭제 데이터 복원 화면, 고도화된 예측 모델, ERP 직접 연동, 자동 메일은 별도 협의 대상이다.
- 등록·수정·삭제 권한의 세분화와 조회 데이터 복제 방지는 현행 권한 UI 범위를 넘어선다.
- MODEL군·작업구분의 행별 자동 분류 규칙은 현재 제공하지 않는다.

## 16. 문서 근거와 변경 관리

사용자와의 구축·수정 대화 및 현행 `app/auth.py`, `app/main.py`, `app/pages`, `app/services`, `app/table_ui.py`, `app/chart_labels.py`, `app/dashboard.css`, `.streamlit/config.toml`을 기준으로 작성했다. 과거 README의 이메일 전환·초기 로고 등 현행과 다른 안내는 최종 요청과 코드 기준으로 정정하여 기술했다.

후속 변경 시 요구사항 식별자와 문서 버전을 갱신하고, 변경 코드·검증 결과·원복 범위를 함께 기록한다. 본 문서의 명세 확인과 운영자의 최종 인수 승인은 별개이다.

## 17. AI 분석과 개인 API 키

### AI-01 접근 권한과 기본 분석

AI 분석 메뉴와 재고현황·완료이력·비용리포트의 조회 권한을 모두 가진 승인 사용자만 분석한다. 매 실행 시 DB의 현재 승인·권한을 확인한다. 분석 결과 다운로드는 AI 분석 및 원본 세 메뉴의 다운로드 권한을 모두 요구한다.

카테고리, 분석 시작일, 종료일을 선택하고 ‘분석 실행’을 누른다. 기본 날짜는 이번 달 1일부터 오늘까지이다. 기간 내 입고 건수·완료 건수·공수 합계·완료 공수 미등록 건수를 계산한다. 이 화면의 건수는 행 수이며 제품 수량 합계와 구별한다. 기본 통계 실행은 외부 API를 호출하지 않는다.

### AI-02 확인 대상과 근거

- 재고 우선 확인: 현재 재고를 입고 경과일 내림차순으로 제공한다. 완료 가능일을 예측하지 않는다.
- 데이터 품질: 입고일 누락, 완료일 누락, 완료 공수 누락, 완료수량 누락 또는 0, 완료일이 입고일보다 빠른 항목을 표시한다. 한 행에 여러 문제가 있으면 중복 표시할 수 있다.
- 공수 이상 후보: 선택 기간의 완료 항목 중 유효한 0 이상 공수와 양수 완료수량을 가진 항목을 비교한다. 대당공수 = 투입공수 / 완료수량이다.
- 동일 모델명·구분 그룹이 5건 이상일 때 Q1 - 1.5×IQR 미만 또는 Q3 + 1.5×IQR 초과를 후보로 표시한다. 하한은 0 이상으로 제한한다. 비교 부족 건수를 별도 안내하고 후보를 오류로 단정하지 않는다.
- 시리얼 반복 기록: 현재 카테고리 전체에서 비어 있지 않은 같은 시리얼이 여러 번 나타나는 기록을 보여준다. 중복 입력인지 반복 재작업인지는 사람이 확인한다.
- 재고·데이터 품질·시리얼 반복은 현재 전체 데이터, 입고·완료·공수 이상은 선택 기간이라는 범위를 화면에 명시한다.

### AI-03 개인 키 입력과 안내

‘개인 API 키 · AI 기능 확장’ 영역에 비밀번호 형식의 개인 OpenAI API 키 입력란과 ‘키 지우기’를 둔다. 다른 공급사의 키를 지원한다고 표시하지 않는다.

안내 문구: “개인 OpenAI API 키를 입력하면 통계 결과 해설, 이상 원인 가설, 재고·데이터 품질 개선 제안 등 AI 기능을 더 폭넓게 사용할 수 있습니다. 키 없이도 기본 통계·이상탐지 분석을 이용할 수 있습니다.”

키는 현재 로그인 사용자 세션에서만 사용한다. DB·파일·로그·공유 캐시·다운로드에 저장하지 않는다. 로그아웃 또는 키 지우기 시 삭제하며 사용자 변경 시 이전 키·답변·전송 동의를 제거한다. 영구 저장 기능이 아니므로 세션 재연결이나 화면 이동 등에 따라 재입력이 필요할 수 있음을 안내한다.

### AI-04 외부 전송과 실행

- 기본 분석 결과에서 종합 해설, 공수 이상 원인 가설, 재고 관리 개선 제안, 데이터 품질 개선 제안 중 관점을 선택한다.
- ‘AI에 전송할 집계 내용 확인’에서 실제 전송 수치를 보여준다. 사용자가 전송에 동의하고 ‘AI 해설 실행’을 누른 경우에만 호출한다.
- 전송 허용값은 기간·기준일, 입고/완료 건수, 공수 합계, 공수 누락 건수, 재고 행수, 품질 확인사항 수, 이상 후보 건수, 비교 부족 건수, 반복 시리얼 해당 행수이다.
- 이름·SITE·시리얼·모델별 원문·업무 원본 행·첨부 마스터는 전송하지 않는다. 키는 인증 헤더로만 사용한다.
- 개인 API 계정에 요금이 발생할 수 있고 제공사의 데이터 처리 정책이 적용됨을 안내한다. 선택적 AI 해설은 외부 전송이 발생하므로 ‘모든 분석은 외부 전송 없음’이라고 표시하지 않는다.
- 현재 연결은 https://api.openai.com/v1/responses, 모델 gpt-4.1-mini, store=false, 최대 출력 1,800토큰, 연결 제한 45초이다. 임의 주소로 인증정보를 보내거나 리디렉션하지 않는다.
- store=false를 외부 제공사의 완전한 무보관 보장이라고 설명하지 않는다.
- 답변은 한국어로 사실·원인 가설·확인사항·제안을 구분한다. 근거 없는 추세·개별 원인·수치를 만들지 않도록 지시한다. 업무 데이터를 자동 수정하지 않는다.
- 키 오류·권한 부족·한도/잔액·연결 실패는 안전한 안내문으로 처리한다. API 원문 오류에 포함될 수 있는 민감정보를 노출하지 않는다. 자동 재시도로 비용을 중복 발생시키지 않는다.

### AI-05 검증 범위

개인 키 분리·삭제, 명시적 실행 전 무호출, 집계 전송 허용목록, 인증 헤더, store=false, 응답 표시와 오류 메시지는 모의 응답 테스트로 확인했다. 실제 유효 키를 이용한 유료 API 응답·청구 및 모델 접근 가능 여부는 미검증이며 운영자가 본인의 키로 확인해야 한다.

## 18. 데이터 모델 및 이관 구현 지침

원본 첨부의 초기 CREATE TABLE 문만 실행하고 최신 스키마가 완성됐다고 판단하지 말라. 현재 app/db.py의 ensure_schema와 각 서비스의 추가 스키마 적용을 함께 확인하라. 운영 DB에 초기 DDL을 재실행하거나 기존 테이블을 무조건 삭제하지 말라.

### DB-01 주요 업무 필드

rework_items는 id, category, source_no, 입고일, 담당자, 담당팀, site, 재작업월, 재작업일, 품목분류, 모델명, serial, 변경모델명, 변경serial, 투입공수, 작업주체, 입고수량, 완료수량, 구분, 재작업내용, 비고_원문, status 및 생성·수정 메타데이터를 가진다. category는 완제품/대여, status는 검수대기/확정/완료이다. 완료 규칙과 저장 상태가 일치하도록 정규화하고 기존 데이터를 검증한다.

### DB-02 계정·관리 정보

- users: 회사명·이름·팀·직책·login_id·email·password_hash·role·approved·can_upload·permissions. role은 관리자/담당자/일반 3종이다. permissions는 메뉴별 권한 JSON이며 기존 업로드 허용값은 호환 처리한다.
- managers: 이름·담당팀(team)·사용 여부. 이름 중복을 허용하지 않는다.
- settings: key·category·value. 카테고리별 key는 ‘설정키|완제품’ 또는 ‘설정키|대여’로 저장하여 기본키 충돌을 방지한다.
- upload_logs와 import_batches: 업로드 결과 및 파일 내용 해시·카테고리 중복 가드.
- site_flags: 검수 확인 대상과 처리 상태.
- 로그인 세션, 비밀번호 재설정 코드, 삭제 감사 이력은 해당 서비스 스키마를 적용한다. 토큰·코드는 해시 저장하며 실제 값을 문서·테스트 산출물에 기록하지 않는다.

### DB-03 초기값과 운영값

신규 설치의 장기재고 기본값은 카테고리별 180일, 제외 구분은 빈 목록, 분당임율 547, MODEL군 DISPLAY, 작업구분 재작업이다. 사용 중인 DB의 90일 등 변경값은 그대로 보존한다. 설정값을 초기값으로 매번 덮어쓰지 않는다. MODEL군·작업구분은 비용 출력용이며 재고 표의 ‘구분’과 별개이다.

### MIG-01 원본 매핑

완제품은 ‘완제품재작업현황’, 대여는 ‘대여작업현황’ 시트를 읽으며 헤더는 3행(0부터 세면 2)이다. 실제 원본에 맞춰 mapping_config.py를 사용하고 컬럼명 개행을 보존·정규화한다.

- 완제품 ‘사이트명’, 대여 ‘대여처’는 공통 site로 연결한다.
- No.는 source_no, Ser.는 serial, 변경Ser.는 변경serial, 비고는 비고_원문으로 연결한다.
- 품목 분류·투입 공수·작업 주체·입고 수량·완료 수량은 원본 개행이 있는 표제와 대응한다.
- 입고년도·입고월은 별도 업무 저장값으로 중복 적재하지 않으며 출력 시 입고일을 기준으로 구성한다.
- 날짜는 지원되는 엑셀 날짜·날짜 표현을 정규화하고 실패값을 검수 대상으로 기록한다. 숫자는 안전하게 변환하며 결측과 0을 구분한다.
- 완전 공백과 번호만 있는 행을 제외한다. 중간 공백 뒤의 유효 업무행은 버리지 않는다. 완료 판정에 공수의 존재 여부를 사용하지 않는다.
- 중복 파일은 해시 및 카테고리로 방지한다. 다른 파일의 개별 중복 행까지 자동 병합한다고 가정하지 않는다.
- 이관 결과는 카테고리별 유효행·입고수량·완료수량·완료/재고·날짜 실패를 원본과 대조한다. 고정 과거 행 수를 맞추려고 행을 임의 추가·삭제하지 않는다.

## 19. 웹주소 배포 및 접근 보호

승인된 사용자는 추후 발급되는 웹주소에서 로그인해 이용한다. 회사 내부 서버를 반드시 사용할 필요는 없지만 웹주소만 입력한다고 호스팅 서버가 생성되는 것은 아니다. 현재 로컬 주소는 운영 공용 주소가 아니다. 도메인 연결·실제 외부 배포는 아직 수행하지 않았다.

- Python 실행, 영구 디스크, HTTPS, WebSocket을 제공하는 외부 호스팅에 설치한다.
- SQLite 운영은 단일 앱 인스턴스를 기준으로 한다. 다중 인스턴스 확장은 별도 DB 전환·검증을 거친다.
- 외부 공개 전에 최초 관리자 계정을 설정한다. 빈 DB 상태의 관리자 초기화 화면을 공개하지 않는다.
- DB·원본·백업은 영구 저장하고 정적 웹 경로로 공개하지 않는다. 비밀 키를 저장소에 넣지 않는다.
- /rework-dashboard/ 경로와 로그인 쿠키 경로를 일치시킨다. CORS/XSRF 보호를 유지하고 HTTPS에서는 Secure 쿠키를 사용한다.
- 미로그인·미승인 사용자 및 권한 없는 메뉴 요청은 서버에서 차단한다. 메뉴 숨김만으로 보호를 대체하지 않는다.
- 조회 허용은 데이터 열람 허용이다. 화면 캡처 등 모든 복제를 막는 기능이나 완전한 유출 방지 보장으로 설명하지 않는다.
- 공개 전 로그아웃·승인 취소·권한 변경·새로고침·토큰 만료·백업 복구·서버 재시작 후 영구 보존을 검증한다.

로컬 실행 명령:

`python -m streamlit run app/main.py --server.address 127.0.0.1 --server.port 8511 --server.baseUrlPath rework-dashboard --server.headless true`

외부 호스팅은 위 주소 바인딩을 서버 환경에 맞게 설정하고 HTTPS 프록시 뒤에서 제공한다. 로컬 실행을 외부 배포 완료로 보고하지 않는다.

## 20. 후속 구현 순서와 최종 인수

### 개발 AI에게 전달할 실행 지침

1. 기존 프로젝트와 운영 문서·코드·설정의 차이를 먼저 확인하라. 본 문서를 새 프로젝트 생성 지시로 해석하지 말라.
2. 수정 대상 코드 및 필요한 데이터의 복구 가능한 백업을 준비하라. 운영 데이터로 파괴적 테스트를 실행하지 말라.
3. 완료 판정·담당팀 연결·권한 검사를 공통 서비스에서 유지하고 화면마다 상이한 계산을 중복 구현하지 말라.
4. DB 변경은 기존 행·계정·참조를 보존하는 마이그레이션으로 적용하라. 초기화가 필요한 경우 임시 테스트 DB만 사용하라.
5. 인증/권한, 업로드/검수, 재고/완료, 비용 출력, 공통 UI, AI 순으로 영향 범위를 검증하라. 이미 구현된 기능을 다시 만들기보다 회귀 여부를 확인하라.
6. 테스트 결과, 변경한 파일, 남은 제한과 실제 운영 확인 필요 항목을 보고하라. Git 저장소가 없는 환경에서 커밋했다고 주장하지 말라.

### 추가 인수 시나리오

- 일반은 조회만, 담당자는 조회·다운로드, 관리자는 모든 작업을 기본 허용하는지 확인한다. 개별 권한 회수 후 기존 세션에서도 제한되는지 확인한다.
- 표 필터를 바꾼 뒤 전체 선택이 원래 행 위치에 잘못 적용되지 않는지, 다운로드 행 수가 대상 범위와 일치하는지 확인한다.
- 완료수량 1·공수 공란·완료일 공란을 포함한 대표 사례를 원본 마스터와 대조한다.
- 정식 비용 파일의 두 시트, 16열 재작업현황 및 21열 대여반납, 금액 반올림과 공란 처리를 확인한다.
- AI 기본 분석만 실행하면 외부 호출이 없는지, 동의 없는 AI 실행이 막히는지, 키가 다른 사용자의 화면·DB·파일에 남지 않는지 확인한다.
- 네트워크 실패·잘못된 키·사용 한도 초과에서도 업무 통계 기능을 계속 사용할 수 있는지 확인한다.
- PDF·명세 작성 완료와 실제 공용 웹 배포·유료 API 실사용 인수를 구별한다.

### 검토 근거와 남은 확인

첨부 구현프롬프트, 2026-09-15 요구사항 명세서, 후속 사용자 요청 및 2026-09-16 현행 코드를 대조하여 개정했다. 특히 DB 기본값·완료 함수·이관 빈행 처리·권한 서비스·비용 출력·개인 API 구현을 확인했다. 이 작업은 문서 개정이며 운영 데이터와 대시보드 기능을 추가 변경하지 않았다.

직전 AI 기능 작업에서 화면·권한 검사 6건과 신규 AI 관련 검사 5건이 통과했다. 이는 전체 업무의 최종 인수 완료를 뜻하지 않는다. 유효한 개인 키로 실제 AI 응답을 확인하는 작업과 외부 호스팅 배포 검증은 남아 있다.

API 구현 참고: OpenAI Responses API 문서 https://developers.openai.com/api/reference/cli/resources/responses/methods/create 및 모델 문서 https://developers.openai.com/api/docs/models/gpt-4.1-mini . 모델 제공·요금·정책은 운영 시 공식 문서로 재확인한다.
