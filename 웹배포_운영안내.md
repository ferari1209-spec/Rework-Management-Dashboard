# 웹주소 접속 운영 안내

웹주소는 앱이 설치된 호스팅 서버에 연결해야 합니다. 주소만 생성해도 서버가 자동 제공되는 것은 아닙니다. 현재 앱은 로컬 실행 상태이며 공용 웹 배포는 아직 수행하지 않았습니다.

## 외부 호스팅 준비

- Python 실행과 영구 디스크를 제공하는 서버에 이 프로젝트를 설치합니다.
- 운영 DB와 data/raw를 영구 디스크에 보관하고 별도로 백업합니다. SQLite는 단일 앱 인스턴스 기준으로 운영합니다.
- 최초 관리자는 공개 전에 설정하고, 운영 DB가 없는 상태로 공개하지 않습니다.
- 호스팅 프록시에서 HTTPS 및 WebSocket을 지원하도록 구성합니다.
- 실행 예: `python -m streamlit run app/main.py --server.address 0.0.0.0 --server.port 8511 --server.baseUrlPath rework-dashboard --server.headless true`
- 외부 주소는 `https://선택한도메인/rework-dashboard/` 경로로 연결합니다. 로그인 쿠키도 이 경로를 사용하므로 경로를 임의 변경하지 않습니다.
- CORS/XSRF 보호를 해제하지 않습니다. DB·원본 파일·백업 폴더를 정적 웹파일로 공개하지 않습니다.
- 계정 승인과 역할·개별 권한은 앱에서 관리합니다. URL을 알고 있어도 미로그인·미승인 계정은 업무 데이터를 열람할 수 없습니다.

## 분석

AI 분석의 기본 통계·규칙 기반 기능은 API 키 없이 서버 내부에서 계산합니다. 개인 OpenAI API 키를 입력하면 집계 결과 해설, 원인 가설, 개선 제안을 추가로 받을 수 있습니다. 호스팅을 사용하면 업무 데이터의 저장 위치는 해당 호스팅 서버가 됩니다.

개인 키는 현재 사용자 세션에만 보관하며 DB·파일·공유 캐시에 저장하지 않습니다. 로그아웃 또는 키 지우기로 제거되며 세션 재연결 시 재입력이 필요할 수 있습니다. 공개 운영에서는 반드시 HTTPS를 사용합니다. 사용자마다 본인의 OpenAI API 키와 사용 한도가 적용됩니다.

집계 전송에 동의하고 AI 해설 실행을 누른 경우에만 OpenAI Responses API(gpt-4.1-mini)를 호출합니다. 전송 내용을 미리 확인할 수 있으며 이름·SITE·시리얼·원본 행은 제외합니다. store=false로 응답 저장을 요청하지 않지만 제공사의 데이터 처리 정책은 적용됩니다. 기본 분석 버튼과 페이지 조회는 외부 호출을 하지 않습니다. 실제 API 사용 요금은 개인 API 계정에 부과될 수 있습니다.

분석은 AI 분석 및 재고현황·완료이력·비용리포트 조회 권한을 모두 요구합니다. 결과 다운로드도 분석과 원본 세 메뉴의 다운로드 권한을 모두 요구합니다. 관리자는 설정에서 AI 분석 권한을 조정할 수 있습니다.

인수 확인: 미승인 로그인 차단, 일반 사용자 다운로드 제한, 메뉴 권한 제거 후 분석 차단, HTTPS 로그인/새로고침/로그아웃, 운영 데이터 영구 보존을 실제 배포 환경에서 확인합니다.
