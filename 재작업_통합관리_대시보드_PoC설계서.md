# AI기반 재작업 통합 관리 대시보드 — PoC 설계서

> 작성일: 2026-09-09
> 근거 자료: 실제 마스터 파일(완제품 5,918행 / 대여 1,664행), 공수비용 산출 양식, 참고 대시보드 디자인 5종, 사전 인터뷰(질문 1~24)

---

## 0. 참고 디자인에서 반영한 스타일 요소 (레이아웃) / 색상은 밝고 깔끔하게 재구성

업로드해주신 참고 이미지(ATEC LGMS/재고 대시보드)에서 **레이아웃 구조**는 가져오되, **색상은 요청에 따라 다크 톤 대신 밝고 편안한 톤**으로 재구성했습니다.

- **상단 헤더**: 화이트 배경 + ATEC 로고(제공 이미지 그대로 사용) + 타이틀 + 우측 필터바
- **KPI 카드**: 큰 숫자 + 보조 지표 + "상세보기 →" 링크 (다크 카드 → 파스텔 배경 카드로 변경)
- **탭 메뉴 구조**: 개요 / 업로드·검수 / 재고현황 / 비용리포트 / 설정
- **도넛차트 + 가로 막대**: 상태 분포를 색상 구분 비율로 표시
- **알람 배지**: 건수를 색상 배지로 강조, 클릭 시 상세 리스트로 드릴다운
- **로그인 화면**: "승인된 계정만 이용 가능" 안내 문구 + 회원가입/로그인 분리

이 구조를 **완제품재작업 / 대여재작업** 두 카테고리에 동일하게 적용하되, 카테고리 전환은 최상단 탭으로 구성합니다.

### 색상 팔레트 (밝고 깔끔한 톤)

| 용도 | 배경색 | 텍스트/강조색 | 의미 |
|---|---|---|---|
| 페이지 배경 | `#F7F9FB` (라이트 그레이) | — | 전체 배경 |
| 카드/헤더 배경 | `#FFFFFF` | — | 콘텐츠 표면 |
| 정보/기본 강조 | `#EAF3FB` (연한 블루) | `#0C447C` (진한 블루) | 총 재고, 일반 정보 |
| 주의/장기재고 | `#FAEEDA` (연한 앰버) | `#633806` (진한 앰버) | 장기재고, 경과일 임박 |
| 완료/정상 | `#E1F5EE` (연한 틸) | `#085041` (진한 틸) | 완료 수량, 정상 상태 |
| 경고/검수대기 | `#FCEBEB` (연한 레드) | `#791F1F` (진한 레드) | 검수 대기, 오류 |

- 그림자 없이 **파스텔 배경 + 진한 텍스트** 조합으로 눈부심 없이 가독성 확보
- 카드 모서리는 10~12px 라운드, 테두리는 최소화(색 배경으로 구분)
- ATEC 로고는 제공된 원본 이미지 파일을 그대로 사용 (재제작하지 않음)

---

## 1. 정보 구조 (IA) / 화면 구성

```
로그인 (승인제)
 └─ 카테고리 선택 (완제품재작업 | 대여재작업)   ← 최상단 고정 탭
     ├─ 1. 개요(대시보드 홈)
     │    - KPI 카드: 총 재고수량 / 장기재고 건수·금액 / 이번달 입고 / 이번달 완료 / 검수 대기 건수
     │    - 장기재고 연령 분포 (0~3개월/3~6/6~12/12+ 구간 막대)
     │    - 담당팀별·SITE별 재고 비중 (도넛/막대)
     │    - 월별 입고 vs 완료 추이 (라인 차트, 최근 12개월)
     ├─ 2. 업로드 & 검수
     │    - ERP 원본(비고란 포함) 업로드
     │    - 자동 파싱 결과 미리보기 (SITE 이상치 하이라이트 / 담당자명 목록 대조 하이라이트)
     │    - 담당자 1회 검수 화면 (행 단위 수정, 일괄 승인)
     │    - 마스터 반영 확정 버튼
     ├─ 3. 재고 현황
     │    - 전체 목록 (필터: 모델명/담당자/SITE/담당팀/구분/입고기간)
     │    - 장기재고만 보기 토글
     │    - 입고일 개별수정 / 선택행 일괄수정
     │    - 완료 처리 (체크 또는 완료일 수기입력)
     │    - 필터링 결과만 엑셀 다운로드
     ├─ 4. 완료 이력
     │    - 완료된 건 누적 조회 (기간 필터)
     │    - 월별 완료수량 / 입고수량 비교 차트
     ├─ 5. 비용 리포트
     │    - 기간 설정 (from~to)
     │    - 분당임율 표시/수정 (기본 547원)
     │    - "재작업현황"(완제품) + "대여반납"(대여) 2개 시트 자동 계산
     │    - 통합 엑셀 다운로드 (1파일 2시트)
     ├─ 6. 설정 (관리자 전용)
     │    - 장기재고 기준일수 (완제품/대여 개별)
     │    - 재고 제외 "구분" 값 지정 (예: 폐기)
     │    - 담당자명 관리 목록 편집
     │    - MODEL군 / 작업구분 항목 추가
     │    - 분당임율 설정
     │    - 사용자 승인 및 권한 관리(관리자/일반, 업로드 권한 개별 부여)
     └─ 7. 마스터 파일 다운로드 (백업용, 원본과 동일 양식)
```

---

## 2. 데이터 모델

### 2.1 핵심 테이블: `rework_items` (완제품/대여 각각 별도 테이블 또는 category 컬럼으로 구분)

| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | PK | 내부 고유 ID |
| category | enum(완제품, 대여) | 카테고리 |
| no | int | 원본 No. (참고용) |
| 입고일 | date | = 이관/업로드일, 수정 가능 |
| 담당자 | string | 담당자명 관리 목록과 매칭 |
| 담당팀 | string | 비고란 파싱 결과 |
| site | string | 대여처/사이트명, 이상치 검증 대상 |
| 재작업월 | string | 완료 시 자동 기록 |
| 재작업일(완료일) | date, nullable | **NULL이면 미완료(=현재 재고)** |
| 품목분류 | string | |
| 모델명 | string | |
| serial | string | 비고란 파싱 결과 |
| 변경모델명 / 변경serial | string, nullable | |
| 투입공수 | float | 비용 계산 기준 |
| 작업주체 | string | |
| 입고수량 | int | |
| 완료수량 | int, nullable | |
| 구분 | string | 해체/출고/완제품화/폐기 등, 재고 제외 여부는 설정값 참조 |
| 재작업내용 | string | |
| 비고_원문 | string | 파싱 이전 원문 보존(추적용) |
| status | enum(검수대기, 확정, 완료) | 업로드 후 워크플로우 상태 |
| created_at / updated_at / updated_by | | 이력 추적 |

### 2.2 설정 테이블 `settings`
- `long_term_threshold_days` (category별)
- `excluded_status_values` (구분 값 중 재고 제외 대상 목록)
- `minute_wage_rate` (기본 547)
- `model_group_options` (기본 DISPLAY + 추가값)
- `work_type_options` (기본 재작업 + 추가값)

### 2.3 마스터 목록 테이블
- `managers` (담당자명, 활성여부) — 편집 가능
- `sites_flagged` (이상치 판정 이력, 확인여부)

### 2.4 사용자/권한 테이블
- `users` (회사명, 이름, 팀, 직책, 로그인ID, 승인상태, role: 관리자/일반)
- `permissions` (user_id, category, can_upload, can_edit 등 세부 플래그 — 확장용)

### 2.5 업로드 이력 테이블 `upload_logs`
- 업로드 일시, 업로드자, 파일명, 반영 행 수, 상태(검수중/확정)

---

## 3. 핵심 처리 흐름

### 3.1 업로드 → 파싱 → 검수 → 반영
```
[ERP 원본 업로드]
   ↓
[자동 파싱] 비고란을 쉼표 기준 3분할 → SITE / 담당자 / 시리얼번호
   ↓
[자동 검증]
   - 담당자명: 관리 목록에 없으면 하이라이트
   - SITE명: 형식 이상치(빈값, 특수문자만, 지나치게 짧음 등 규칙) 하이라이트
   - 누락값(SITE/담당자/시리얼) 하이라이트
   ↓
[담당자 1회 검수 화면] ← ★사람 판단 단계
   - 하이라이트된 행 위주로 확인/수정
   - 담당자명 목록에 새 이름 즉시 추가 가능
   ↓
[확정] → status: 검수대기 → 확정, 마스터 반영, 입고일 = 업로드일 기록
```

### 3.2 재고/장기재고 판정 (자동, 배치 또는 조회 시 실시간 계산)
```
IF 재작업일(완료일) IS NULL
   AND 구분 NOT IN (설정된 제외 대상)
THEN 재고 항목으로 집계

장기재고 여부 = (오늘 - 입고일) >= 설정된 기준일수 (카테고리별)
```

### 3.3 완료 처리 ← ★사람 판단 단계
```
담당자가 완료 체크 또는 완료일 수기 입력
   → status: 확정 → 완료
   → 재작업일/완료수량/구분 등 기록
   → 완료 이력 테이블에 누적 (삭제 아님, 재고 집계에서만 제외)
```

### 3.4 월별 지표 계산
- 월별 입고수량 = 해당월 입고일 기준 합계
- 월재작업수량(=월별 완료수량) = 해당월 재작업일(완료일) 기준 합계

### 3.5 비용 리포트 생성
```
[기간 설정: from ~ to]
   ↓
완제품 마스터에서 해당 기간(재작업일 기준) 데이터 추출
   → "재작업현황" 시트 포맷 매핑
     계획날짜=재작업일의 연월 / 작업날짜=재작업일 / MODEL군=기본값(설정 가능)
     작업구분=기본값(설정 가능) / 귀책부서=담당팀 / 금액=투입공수×분당임율×60
     (사유/귀책사유정리/전산공수반영여부/LOSS실공수/품질보증팀귀책확정 → 공란)
   ↓
대여 마스터에서 동일 기간 데이터 추출
   → "대여반납" 시트 포맷 매핑 (기존 마스터 컬럼 + 금액 컬럼 삽입)
   ↓
[하나의 엑셀 파일, 시트 2개로 다운로드]
```

### 3.6 기존 데이터 마이그레이션
```
완제품/대여 마스터(.xls, 헤더 3행) → 실데이터 행까지 파싱 (빈 행 이후 컷)
   → rework_items 테이블에 일괄 적재
   → status = 완료(재작업일 있는 행) / 확정(재작업일 없는 행, 현재 재고)
```

---

## 4. 사람이 반드시 개입하는 단계 (자동화 대상 아님, UI만 지원)

| 단계 | 내용 | 화면 |
|---|---|---|
| ① 업로드 후 검수 | 파싱 결과 오탈자/누락 확인·수정 (SITE 이상치, 담당자명 목록 대조) | 업로드 & 검수 |
| ② 완료 처리 판단 | 재작업 완료 여부를 담당자가 직접 체크/입력 | 재고 현황 |
| ③ 재고 제외 유형 지정 | "구분" 값 중 어떤 것을 재고에서 뺄지 결정 | 설정 |
| ④ 신규 사용자 승인 | 가입 신청자를 관리자가 승인 | 설정 |

---

## 5. 4일 MVP 범위 제안 (질문 진행 시 언급한 범위와 정합)

| 일자 | 작업 |
|---|---|
| 1일차 | DB 스키마 구성(SQLite), 기존 마스터 2개 파일 마이그레이션 스크립트, 로그인/승인 기본 구조 |
| 2일차 | 업로드→파싱(비고란 분리)→검수 화면, 확정 반영 로직 |
| 3일차 | 개요 대시보드(KPI/차트), 재고현황(필터/완료처리/입고일수정), 완료이력 |
| 4일차 | 비용 리포트(기간설정→2시트 다운로드), 설정 화면, 마스터 다운로드, 통합 테스트 |

**MVP 제외(2단계 확장 권장)**: 세부 권한 커스터마이징 UI, 대여기록 대비 예측 지표, 알림(이메일/슬랙) 연동

---

## 6. 다음 단계 제안

이 설계서를 기준으로 Claude Code에서 작업 단위를 다음과 같이 쪼개어 순차 구현하는 것을 권장합니다.

1. `schema.sql` + 마이그레이션 스크립트 작성 → 실제 파일로 검증
2. Streamlit 페이지 뼈대(카테고리 탭, 사이드바 메뉴) 구성
3. 업로드·파싱·검수 로직 구현 (단위 테스트: 비고란 파싱 케이스 다양화)
4. 대시보드 시각화 구현
5. 비용 리포트 생성 로직 구현 (금액 계산 검증: 0.8×547×60=26,256원 등 기존 값과 대조)
6. 로그인/권한 구현

필요하시면 이 설계서를 기반으로 **1단계(DB 스키마 + 마이그레이션)**부터 바로 Claude Code 작업 지시서 형태로 만들어 드리겠습니다. 진행할까요?
